# 验证记录

## 自动验证

`go test ./...` 已通过：暂停 / 继续、重复开始幂等性、休眠后的到期合并、内容与间隔修改、重启恢复、保存失败回滚、损坏配置备份、非法值拒绝、通知失败恢复、测试通知隔离、暂停取消在途通知、中文 / 特殊字符与强制静音 XML。

`node --check frontend/app.js` 已通过。

## 桌面验证步骤

1. 执行 `wails dev`，窗口显示初始配置与未开始状态。
2. 修改内容和间隔，检查自动保存；开始后倒计时减少。
3. 暂停，等待后确认倒计时不变；继续后接着计时。
4. 点击无声测试通知，确认 Windows 接收并显示中文提醒；点击“系统通知设置”检查快捷入口。
5. 设为 1 分钟并最小化，等待实际周期，确认后台计时。
6. 点击叉号，确认程序隐藏到托盘且继续计时；托盘右键“打开主窗口”恢复。
7. 托盘右键“退出应用”，重新打开 exe，检查恢复上次内容与间隔，且没有自动开始。
8. 同时再次打开 exe，确认只有一个应用窗口。
9. 执行 `build.bat`，检查分发 ZIP 中的程序、使用说明与第三方许可。

## 已完成的实际验证与边界

2026-09-07，Windows 本机 Go 1.25.7 / Wails 2.15.0：

- `wails dev` 成功启动真实 WebView2 桌面窗口，绑定和自动读取配置正常。
- `wails build` 成功生成 Windows amd64 exe；分发目录中的独立 exe 已成功运行。
- 原生窗口控件读取确认内容和 1 分钟间隔在重新打开后恢复，且处于未开始状态。
- 开发界面中观察到实际倒计时、暂停后的剩余时间和一分钟到期处理。
- 首次到期发现 WinRT 通知权限状态返回空值；原来将空值误判为关闭，已修复为仅明确禁用时阻止发送。
- 用户随后停止桌面自动操作，因此修复后的通知横幅、1.1.0 新增托盘菜单及设置按钮未继续进行原生界面实测。不能将本地编译通过视为 Windows 已显示通知。
- 布局通过相同前端资源的本地浏览器检查；原生截图接口在本机返回不支持接口，原生控件读取可用。
- 最终 1.1.0 代码的 `go test ./...`、`go test -run '^$' ./...`、JavaScript 语法检查和 `git diff --check` 均通过。
- 1.1.0 的构建脚本已成功生成 amd64 exe 与包含第三方许可的分发 ZIP。依赖缓存完成后，以 `GOPROXY=off`、`GOSUMDB=off` 完成了最终测试与重新构建。
