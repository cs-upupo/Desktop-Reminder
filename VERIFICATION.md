# 验证记录

## 自动验证范围

- 多个任务在同一时刻分别触发通知。
- 循环任务暂停后保留剩余时间，继续后不重置；只编辑内容不重置倒计时，改变计划后使用新的完整间隔。
- 每日任务首次提醒后持续间隔提醒，完成后自动安排次日。
- 一次性任务持续提醒到完成，完成后必须编辑新时间才能再次启用。
- 任务完成后写入指定日期记录；删除任务后仍保留完成记录。
- 旧版单任务配置自动迁移为暂停的循环任务。
- 非法名称、时间和分钟数被拒绝；通知失败会保留待完成状态并显示错误。
- 通知 XML 对中文和特殊字符正确转义，并明确静音。
- JavaScript 语法、全部 Go 测试、全部 Go 包编译和 Wails Windows amd64 构建。

## 桌面验证步骤

1. 启动开发窗口，分别新建一个 1 分钟循环任务、一个临近时间的每日任务和一个临近时间的一次性任务。
2. 检查三个任务分别显示计划、状态和倒计时；编辑内容后倒计时不重置。
3. 暂停和继续循环任务，确认剩余时间保留；改变间隔后确认从新间隔开始。
4. 等待任务首次到期，确认收到无声 Windows 通知，任务变为“待完成”，并按设置间隔再次提醒。
5. 点击完成：循环和一次性任务停止；每日任务自动显示下一天的时间。
6. 进入“日期记录”，用前一天、今天、后一天以及日期输入框检查任务总数与完成状态。
7. 删除一个已完成任务，再查看原日期，确认完成记录仍保留。
8. 点击“打开 Windows 通知设置”，确认能到达系统通知页面。
9. 点击窗口叉号，确认隐藏到托盘且提醒继续；托盘右键恢复窗口，再从托盘退出。
10. 重新打开，确认多任务、运行状态、待完成状态和日期记录均恢复。
11. 构建分发 ZIP，在独立目录解压并运行其中的 exe。

Windows 勿扰、专注助手或系统通知设置可能隐藏横幅；测试通知成功表示 Windows 接受了通知请求。电脑关机时无法通知，休眠恢复后会处理已经到期的任务。
